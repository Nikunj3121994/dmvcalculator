@model object

<div ng-controller="dmvController">

    <form id="dmvform">
        <input type="text" ng-model="inputData" />
    </form>

    <p>{{inputData}}</p>

    <div>{{first}}</div>

    <ul>
        <li ng-repeat="item in vehicleTypeCodeTable">
            {{item.id}} - {{item.name}}
        </li>
    </ul>
</div>
